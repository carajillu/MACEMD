system:
  initial_structures: ["example.xyz"]
  pbc: True
  cellvectors: [10, 10, 10]

computing:
  local:
    devices: [cpu] # list of devices to use
    omp_num_threads: 1 # number of OpenMP threads to use
    mpi_num_processes: 16

md:
  

